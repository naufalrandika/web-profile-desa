import { createContext } from 'react';
const defaultValue = { graph: null, setGraph: () => { } };
export const GraphContext = createContext(defaultValue);
export const Provider = GraphContext.Provider;
export const Consumer = GraphContext.Consumer;
export default GraphContext;
//# sourceMappingURL=GraphContext.js.map
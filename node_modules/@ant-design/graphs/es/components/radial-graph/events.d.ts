import { GraphData } from '@antv/g6';
import { RadialLayout } from '@antv/layout';
import { ITreeGraph, NodeConfig, FetchLoading } from '../../interface';
/** sector layout */
export declare const bindRadialExplore: (params: {
    graph: ITreeGraph;
    asyncData: (nodeCfg: NodeConfig) => GraphData;
    layout?: RadialLayout;
    fetchLoading?: FetchLoading;
}) => void;

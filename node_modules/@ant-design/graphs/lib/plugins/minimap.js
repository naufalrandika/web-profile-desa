"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.processMinimap = void 0;
var g6_1 = __importDefault(require("@antv/g6"));
var constants_1 = require("../constants");
var processMinimap = function (cfg, graph) {
    if (cfg === void 0) { cfg = {}; }
    if (!graph || graph.destroyed) {
        return;
    }
    var pluginMinimap = graph.get('plugins').filter(function (plugin) { return plugin.get('name') === 'minimap'; })[0];
    if (pluginMinimap) {
        graph.removePlugin(pluginMinimap);
    }
    if (cfg.show) {
        var curMminimapCfg = Object.assign(constants_1.defaultMinimapCfg, cfg);
        var minimap = new g6_1.default.Minimap(__assign(__assign({}, curMminimapCfg), { name: 'minimap' }));
        graph.addPlugin(minimap);
    }
};
exports.processMinimap = processMinimap;

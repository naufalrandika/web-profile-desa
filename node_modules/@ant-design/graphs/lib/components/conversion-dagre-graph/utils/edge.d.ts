import type { Graph, EdgeConfig } from '@antv/g6';
import type { Rankdir } from '../types';
import type { LayerOrder } from '../types';
export declare const getEdgeAnchorAndType: (graph: Graph, edge: EdgeConfig, layerOrder: LayerOrder, rankdir: Rankdir) => {
    type: any;
    sourceAnchor: any;
    targetAnchor: any;
};
export declare const updateEdgeAnchorAndType: (graph: Graph, layerOrder: LayerOrder, rankdir: Rankdir) => void;
export declare const getArrowConfig: (color?: string) => {
    path: string;
    d: number;
    fill: string;
    stroke: string;
    lineWidth: number;
    lineDash: any[];
};

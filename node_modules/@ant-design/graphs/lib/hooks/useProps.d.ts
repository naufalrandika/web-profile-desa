import { CommonConfig } from '../interface';
type SpecialKey = 'level';
export default function useProps(props: CommonConfig, defaultProps: Partial<CommonConfig>): {
    uProps: Partial<CommonConfig> & {
        [key in SpecialKey]?: number;
    };
};
export {};

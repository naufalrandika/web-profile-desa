import { IGraph, IGroup, EdgeConfig, NodeConfig, TreeGraph } from '../interface';
export declare const getCommonCfg: (cfg: unknown, item: EdgeConfig<string | {
    text?: string;
    subText?: string;
}> | NodeConfig | undefined, graph?: IGraph | IGroup | TreeGraph | undefined) => any;

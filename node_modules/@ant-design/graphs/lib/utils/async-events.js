"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.runAsyncEvent = exports.pushAsyncEvent = void 0;
var EventMap = new Map();
var pushAsyncEvent = function (eventId, event) {
    var events = EventMap.get(eventId);
    if (!events) {
        events = [event];
    }
    else {
        events.push(event);
    }
    EventMap.set(eventId, events);
};
exports.pushAsyncEvent = pushAsyncEvent;
var runAsyncEvent = function (eventId) {
    var events = EventMap.get(eventId) || [];
    events.forEach(function (event) {
        if (typeof event === 'function')
            event.apply(null, arguments);
    });
    EventMap.set(eventId, []);
};
exports.runAsyncEvent = runAsyncEvent;

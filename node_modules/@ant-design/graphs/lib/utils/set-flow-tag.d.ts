import { FlowGraphDatum } from '../interface';
/**
 * 对数据进行打标，加上 level
 */
export declare const setFlowTag: (data: FlowGraphDatum, limitLevel?: number) => {
    nodes: any[];
    edges: import("../interface").FlowGraphEdgeData[];
};
